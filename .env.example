# =============================================
# WishHub Environment Configuration Template
# =============================================
#
# USAGE:
# 1. Copy this file to .env:  cp .env.example .env
# 2. Fill in ALL values below
# 3. NEVER commit .env to git!
#
# For EasyPanel: paste these variables in the Environment section
#
# =============================================
# REQUIRED: Application URL
# =============================================
# Change for your domain (EasyPanel auto-generates SSL)
PUBLIC_URL=https://your_domain.com

# =============================================
# REQUIRED: Database
# =============================================
POSTGRES_DB=wishlist
POSTGRES_USER=wishlist
# Generate strong password: openssl rand -base64 32
POSTGRES_PASSWORD=CHANGE_THIS_PASSWORD

# =============================================
# REQUIRED: JWT Secret
# =============================================
# Generate: openssl rand -hex 32
# IMPORTANT: Must be at least 32 characters!
JWT_SECRET=CHANGE_THIS_TO_64_CHAR_RANDOM_SECRET
JWT_ISSUER=WishHub
JWT_AUDIENCE=WishHubUsers
JWT_ACCESS_TOKEN_EXPIRY_MINUTES=60
JWT_REFRESH_TOKEN_EXPIRY_DAYS=30

# =============================================
# REQUIRED: OAuth VK ID
# =============================================
# Get from: https://id.vk.ru/business/go
# Redirect URI must be: ${PUBLIC_URL}/api/auth/vk/callback
VK_CLIENT_ID=YOUR_VK_APP_ID
VK_CLIENT_SECRET=YOUR_VK_SECRET

# =============================================
# REQUIRED: Telegram Bot
# =============================================
# Get from @BotFather, set domain via /setdomain
TELEGRAM_BOT_TOKEN=YOUR_BOT_TOKEN
TELEGRAM_BOT_USERNAME=your_bot_username

# =============================================
# OPTIONAL: Background Jobs
# =============================================
HANGFIRE_INTERVAL=8

# =============================================
# OPTIONAL: Registry-based deploy (docker-compose.registry.yml)
# =============================================
# Used only when deploying with prebuilt images from GHCR via EasyPanel.
# Build pipeline: GitHub Actions builds & pushes to ghcr.io on every push to main.
# Server pulls the prebuilt image instead of building locally — saves ~5-7 GB
# of build cache and CPU on the VPS.
#
# IMAGE_OWNER  - lowercase GitHub username/organisation that owns the packages
# IMAGE_TAG    - image tag to deploy: "latest" (auto on main), "sha-abc1234",
#                or a release tag like "v1.0.0"
IMAGE_OWNER=your_github_username
IMAGE_TAG=latest
